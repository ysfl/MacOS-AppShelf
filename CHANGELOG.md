# 更新记录

## 1.4.0 - 2026-09-19

### 新增

- 卡片密度偏好：紧凑 / 标准 / 宽松三档，图标尺寸、每行数量与拖动让位距离由同一份栅格参数派生
- 登录时启动应用架，走系统登录项注册；被拒绝时说明原因而不是让开关自己弹回
- 使用记录参与排序：搜索同分时启动更多的排前面，浮层空查询时按运行中、常用、最近、名称排列
- 设置里可清除使用记录、重新载入外部语言文件
- 新增 `Scripts/gates/gates.py`：文件规模、禁桩检查、提交信息、公开内容红线、翻译表完整性、字号棘轮、坑位台账七道门禁
- 新增 `.githooks/` 三段钩子链（pre-commit / commit-msg / pre-push）与 `Scripts/gates/install-hooks.sh`
- 新增 `Scripts/visual-check.sh`：发布前按外观 × 语言组合出图，覆盖单元测试看不见的布局问题
- 新增 `docs/`：按规范、台账、快照三层组织，规范里的阈值与门禁实现一一对应

### 修复

- 静默失败改为可诊断：状态读写、占用缓存、语言表解析、快捷键编码与注册、备份导入导出全部落到统一日志，不再用 `try?` 吞掉解码错误
- 窗口标题跟随应用内语言，Finder 与 Dock 的名字跟随系统语言
- 构建脚本按语种生成 `InfoPlist.strings`

### 工程

- 切到 Swift 6 严格并发语言模式，全新构建零诊断：33 处"只应在主线程用却从未声明"的单例全部补上 `@MainActor`；纯文件系统测量拆到非隔离的 `DiskMeasurement`，`AppMetrics` 归主线程
- 把 2,654 行的 `Views.swift` 按职责拆成 8 个文件，`Models.swift`、`SettingsView.swift`、`SpotlightPanel.swift` 同步拆分；拆分脚本带内容守恒校验，拒绝丢行或重复
- 10 组高频字号-字重组合收敛为命名角色（68 个调用点无损替换），其余 34 处特殊尺寸由棘轮门禁冻结，只准降不准升
- 单元测试从 80 增至 87


## 1.3.1 - 2026-09-19

- 修复标题栏外观与语言菜单被横向拉散：无障碍标签加在 `Menu` 上会让 `.borderlessButton`
  把指示箭头推到可用空间的最远端，改为标注在图标上并锁定尺寸
- 文档同步：安装包与校验和的下载链接改指向 Release 附件（发布产物已不再入库），
  补齐快捷键一览、新增能力的功能条目、数据范围说明与持续集成发布流程


## 1.3.0 - 2026-09-19

### 修复

- 关闭主窗口后，从菜单栏或 Dock 点“打开应用架”现在会真的把窗口重新打开，不再是毫无反应
- 设置里停用聚焦搜索快捷键后不再于下次启动被悄悄改回 ⌥Space
- 扫描应用移到后台执行：刷新时界面不再冻结约 160 毫秒，“正在读取应用…”也能正常显示
- 深色模式下搜索框与快捷工具的描边不再消失，聚焦搜索浮层的描边在浅色模式下也不再消失
- 英文界面下内置分组名不再显示中文，常用、开发等会随语言翻译，用户自建名称保持原样
- 拖动应用时不再把同一应用在其他分组里的卡片一起变透明
- 把分组拖到移除条上松手后，移除条立即收起，不再等 0.35 秒
- 从分组移除应用时的落位动画改为在未分组分区播放，此前播在已经离开的分组上，永远看不见
- 拖动结束后连投两次，第一次的收尾不再掐掉第二次的落位动画
- 拾起卡片时源卡片的淡出改为渐变，不再是硬切
- 磁盘占用取整后再选单位：1048575 字节不再显示成“1024 KB”，1.25 GB 不再显示成“1.2 GB”
- 拼音首字母的精确命中不再输给别名的前缀命中：搜“wx”时微信排到企业微信之前
- 分类关键词改为同时匹配去空格文本，活动监视器等中文名不再落到“其他”
- 设置面板改为可缩放并带滚动，快捷工具变多后不再被裁掉且无法访问
- “正在运行”里没有应用时，空状态不再提示“这个分组还没有应用”
- 浮层用方向键选中超出可见范围的搜索结果时自动滚动到该条目
- 快捷键“停用”状态不再把当时的语言写进显示文本，切换语言后不会留下旧语言的标签
- 外部语言目录新增文件后可在设置里点“重新载入语言”生效，不必重启
- 卸载或移除的应用不再在内存与磁盘缓存里留下永久条目
- 重新计算磁盘占用期间显示剩余项数，并提供取消按钮
- 移除的快捷工具与分组改动可以撤销
- 发布脚本把版本号写入安装包内的 Info.plist，并拒绝打包与文档不一致的版本

### 新增

- 主窗口键盘导航：↑↓ 在结果间移动，回车打开高亮项，esc 退出，⌘F 聚焦搜索，⌘1 至 ⌘9 切换分组，⌘Z 撤销
- 可以把应用从应用架隐藏，隐藏后不出现在列表与搜索中；侧栏新增“已隐藏”页用于找回
- 支持从访达把 .app 直接拖进分组、分区或快捷工具区
- 分组、隐藏名单、快捷工具与偏好可导出为 JSON 备份，也可导入还原
- 分组图标改为可搜索的完整目录，不再只有固定的 15 个
- 卡片右键菜单新增“显示包内容”“拷贝 Bundle ID”
- 全部界面元素补齐无障碍标签与取值，VoiceOver 不再只读到一串无名按钮
- 遵循系统的“减弱动态效果”设置，弹簧位移退化为近即时过渡
- 新增 AppShelfCore 模块与 80 个单元测试，覆盖搜索打分、排序索引、栅格度量、快捷键持久化、翻译回退与备份编解码

### 工程

- 拖拽与排序的索引数学、栅格度量、路径归一、快捷键编解码、翻译回退移入无 UI 的 AppShelfCore，可独立测试
- 应用栅格的列宽与间距只剩一处定义，此前由注释维系两处手写常量同步
- 新增 GitHub Actions：推送与 PR 执行零警告构建、测试并打包 .app；推送 v* 标签时校验校验和并上传 Release
- 安装包不再提交进仓库，改由 Release 工作流产出

## 1.2.0 - 2026-09-18

- 支持把应用卡片直接拖到侧边栏分组完成归类
- 卡片改为启动台样式：大图标、居中名称、一行占用信息
- “全部应用”按分组分区显示，卡片可拖动调整组内顺序，分组本身也能拖动排序
- 快捷工具可直接拖入应用添加，拖出到内容区即可移除
- 搜索支持中文拼音首字母、全拼、英文缩写和模糊匹配，并按相关度排序
- 搜索同时索引 bundle 本地化名与文件名，WeChat 可按“wx”命中，Visual Studio Code 可按“vsc”命中
- 卡片显示磁盘占用；运行中的应用在图标上加运行标记并显示实时内存占用；长名称自动换行
- 右键菜单支持退出应用与强制结束进程
- 快捷工具可自定义：显示、隐藏、排序，并能把任意应用加为快捷工具
- 右键菜单可取消应用的分组设置（移出单个分组或移出全部分组）
- 拖动应用时分组底部出现红色区域，拖进去即从该分组移除
- 应用落入分组时在目标位置播放图标缩小的过渡动画
- 优化渲染：拖拽高亮、底栏、占用信息各自独立更新，避免整页重绘
- Dock 图标右键菜单支持聚焦搜索、打开应用架、分组跳转和设置
- 新增类似 macOS 聚焦的独立搜索浮层：快捷键唤出，回车直接打开应用
- 新增设置面板：可自行录制聚焦搜索快捷键，支持恢复默认、停用和菜单栏图标开关
- 新增菜单栏图标，提供搜索、打开窗口和设置入口

### 1.2.0 新增

- 新增明暗模式切换：可跟随系统，或强制浅色 / 深色
- 新增多语言：内置中文与英文，标题栏可随时切换，无需重启
- 翻译按语种拆分为独立文件，并支持外部覆盖：把 `<语言代码>.json`（如 `ja.json`）放到
  `~/Library/Application Support/AppShelf/Localization/` 即可新增语种，无需重新编译
- 拖动应用时改为整张卡片跟随光标，栅格中留下的位置淡化为落点占位
- 拖动经过其他图标时，沿途图标按 macOS 启动台的方式让位，松手即落在空出的位置
- 拖动时每个卡片显示边缘，分组整块常亮显示落点区域（不再随指针闪烁）
- 分组底部的移除区域改为显示在卡片下方、更高，且仅在拖动时出现
- 磁盘占用改为按卡片懒加载，进程内存扫描移出主线程，列表滚动更顺滑
- 刷新按钮增加确认提示，说明重新扫描可能耗时并占用 CPU / 磁盘
- 搜索时按 esc 退出搜索；搜索结果不允许拖动
- 修复拖拽状态可能卡住的问题：esc、松开鼠标、滚动、切换页面均会退出拖拽状态
- 修复拖动时偶尔需要多跨一个应用才能识别落点的问题

## 1.0.0 - 2026-09-04

首个公开版本。

- 自动扫描用户应用、系统应用和 `~/Applications`
- 支持常用、开发、沟通、创作、日常、工具分组
- 支持新建、编辑、删除分组以及自定义图标和颜色
- 支持搜索、运行中筛选、应用启动和 Finder 定位
- 支持手动加入任意 `.app`
- 提供计算器、终端、活动监视器、截图和系统设置快捷入口
- 发布 macOS 安装包 `AppShelf-1.0.0.dmg`
